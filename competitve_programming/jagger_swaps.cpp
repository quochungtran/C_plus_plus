#include "template.hpp"

void solve() {
    ll n;
    in(n);
    vector<ll> a(n);
    in(a);
    
    if (a[0] == 1){
        print("YES");
    }
    else
    {
        print("NO");
    }

    return;
}